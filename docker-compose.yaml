services:
  postgres:
    image: postgres:14
    container_name: healf-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: healf
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
